# Expected-failure list for the protobuf-conformance suite.
# Covers BOTH the proto3 and the proto2 binary/json suites
# (despite the legacy filename) — the conformance runner
# applies a single failure list to whichever suite it's
# currently running.
#
# Status: 2 expected failures / 2673 successes / 0 skipped /
# 0 unexpected failures.
#
# Today's residue:
#
#   * EnumFieldUnknownValue.Validator (Proto3 Required, JsonInput):
#     proto3 "open enum" semantics ask us to preserve unknown
#     numeric enum values across a JSON round-trip — input
#     '{"optionalNestedEnum": 123}' should re-emit as
#     '{"optionalNestedEnum": 123}' even though 123 isn't a
#     declared NestedEnum value. Doing this faithfully requires
#     representing each generated enum as
#     'data NestedEnum = ... | NestedEnum'Unknown !Int32' and
#     threading the new constructor through every encoder /
#     decoder / JSON path the deriver emits.
#
#   * FieldNameExtension.Validator (Proto2 Recommended, JsonInput):
#     proto2 extensions accessed via the JSON bracket syntax
#     '[protobuf_test_messages.proto2.extension_int32]'.
#     Supporting it requires parser hooks for the
#     '[FQN]'-quoted key form plus extension dispatch from
#     loadProto-generated FromJSON instances. Out of scope for
#     this round.
#
# Update this file when the suite is re-run on a new VM:
#   cabal test wireform-proto:protobuf-conformance-test \
#     2>&1 | tee /tmp/conformance.log
# then mine /tmp/conformance.log for new failure names.

Required.Proto3.JsonInput.EnumFieldUnknownValue.Validator
Recommended.Proto2.JsonInput.FieldNameExtension.Validator
