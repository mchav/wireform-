{-# LANGUAGE StrictData #-}
{-# LANGUAGE DeriveGeneric #-}
{-# LANGUAGE DeriveAnyClass #-}
{-# LANGUAGE DerivingStrategies #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE OverloadedRecordDot #-}
-- | Auto-generated protobuf types from package @temporal.api.compute.v1@.
--
-- __THIS FILE IS AUTO-GENERATED BY wireform. DO NOT EDIT.__
--
-- Any manual changes will be overwritten the next time code
-- generation is run.  To modify the types or instances, edit the
-- @.proto@ source file and re-run the code generator.
module Proto.Temporalio.Api.Compute.V1.Provider where

import Data.ByteString (ByteString)
import qualified Data.ByteString as BS
import qualified Wireform.Builder as B
import Data.Int (Int32, Int64)
import Data.Text (Text)
import qualified Data.Text as T
import Data.Word (Word32, Word64)
import qualified Data.Map.Strict as Map
import qualified Data.Vector as V
import qualified Data.Vector.Unboxed as VU
import GHC.Generics (Generic)
import Control.DeepSeq (NFData(..))
import Data.Hashable (Hashable(..))
import Proto.Encode
import Proto.Decode
import qualified Data.Aeson as Aeson
import qualified Data.Aeson.Types as Aeson
import qualified Data.Aeson.Key as AesonKey
import qualified Data.Aeson.KeyMap as AesonKM
import Proto.Internal.JSON (jsonObject, (.=:), parseFieldMaybe, bytesFieldToJSON, parseBytesFieldMaybe, bytesMapFieldToJSON, parseBytesMapFieldMaybe, protoBytesToJSON)
import Data.Proxy (Proxy(..))
import Proto.Schema (ProtoMessage(..), SomeFieldDescriptor(..), FieldDescriptor(..), FieldTypeDescriptor(..), ScalarFieldType(..), FieldLabel'(..))
import Proto.Registry (IsMessage)
import Proto.Registry qualified
import qualified Proto.Extension
import Proto.Internal.Wire (Tag(..), WireType(..))
import Proto.Internal.Wire.Encode (putTag, putVarint, putFixed32, putFixed64,
  putFloat, putDouble, putText, putByteString, putLengthDelimited,
  putSVarint32, putSVarint64, putVarintSigned,
  varintSize, tagSize, fieldMessageSize,
  fieldVarintSize, fieldFixed32Size, fieldFixed64Size,
  fieldBoolSize, fieldFloatSize, fieldDoubleSize,
  fieldTextSize, fieldBytesSize,
  fieldSVarint32Size, fieldSVarint64Size,
  varintSize32, zigZag32, zigZag64)
import Proto.Internal.Encode.Archetype (archVarint, archSVarint32, archSVarint64,
  archFixed32, archFixed64, archFloat, archDouble, archBool,
  archString, archBytes, archSubmessage,
  archVarintSize, archStringSize, archBytesSize, archBoolSize,
  archFixed32Size, archFixed64Size, archSubmessageSize)
import qualified Proto.Temporalio.Api.Common.V1.Message as TE_Common_V1_Message

-- | Serialized FileDescriptorProto for this .proto file.
-- Decode with @Proto.Google.Protobuf.Descriptor.decodeMessage@.
fileDescriptorProtoBytes :: ByteString
fileDescriptorProtoBytes = ""


-- | ComputeProvider stores information used by a worker control plane controller
-- to respond to worker lifecycle events. For example, when a Task is received
-- on a TaskQueue that has no active pollers, a serverless worker lifecycle
-- controller might need to invoke an AWS Lambda Function that itself ends up
-- calling the SDK's worker.New() function.
data ComputeProvider = ComputeProvider
  { computeProviderType :: !Text
    -- ^ Type of the compute provider. This string is implementation-specific and
    -- can be used by implementations to understand how to interpret the
    -- contents of the provider_details field.
  , computeProviderDetails :: !(Maybe TE_Common_V1_Message.Payload)
    -- ^ Contains provider-specific instructions and configuration.
    -- For server-implemented providers, use the SDK's default content
    -- converter to ensure the server can understand it.
    -- For remote-implemented providers, you might use your own content
    -- converters according to what the remote endpoints understand.
  , computeProviderNexusEndpoint :: !Text
    -- ^ Optional. If the compute provider is a Nexus service, this should point
    -- there.
  , computeProviderUnknownFields :: ![UnknownField]
  }
  deriving stock (Show, Eq, Generic)
  deriving anyclass NFData

defaultComputeProvider :: ComputeProvider
defaultComputeProvider = ComputeProvider
  { computeProviderType = ""
  , computeProviderDetails = Nothing
  , computeProviderNexusEndpoint = ""
  , computeProviderUnknownFields = []
  }

instance MessageEncode ComputeProvider where
  buildMessage msg =
    (if msg.computeProviderType == T.empty then mempty else archString 10 msg.computeProviderType)
    <> (maybe mempty (\v -> let sz = messageSize v in archSubmessage 18 sz (buildMessage v)) msg.computeProviderDetails)
    <> (if msg.computeProviderNexusEndpoint == T.empty then mempty else archString 82 msg.computeProviderNexusEndpoint)
    <> encodeUnknownFields msg.computeProviderUnknownFields

instance MessageSize ComputeProvider where
  messageSize msg =
    (if msg.computeProviderType == T.empty then 0 else archStringSize msg.computeProviderType)
    + (maybe 0 (\v -> archSubmessageSize (messageSize v)) msg.computeProviderDetails)
    + (if msg.computeProviderNexusEndpoint == T.empty then 0 else archStringSize msg.computeProviderNexusEndpoint)
    + unknownFieldsSize msg.computeProviderUnknownFields

instance MessageDecode ComputeProvider where
  {-# INLINE messageDecoder #-}
  messageDecoder = loop "" Nothing "" []
    where
      loop acc_0 acc_1 acc_2 acc_unknown_ = withTagM
        (pure (ComputeProvider {computeProviderType = acc_0, computeProviderDetails = acc_1, computeProviderNexusEndpoint = acc_2, computeProviderUnknownFields = reverse acc_unknown_}))
        (\fn wt -> case fn of
          1 -> do
            v <- decodeFieldString
            loop v acc_1 acc_2 acc_unknown_
          2 -> do
            v <- decodeFieldMessage
            loop acc_0 (Just v) acc_2 acc_unknown_
          10 -> do
            v <- decodeFieldString
            loop acc_0 acc_1 v acc_unknown_
          _ -> do
            uf <- captureUnknownField fn (toEnum wt)
            loop acc_0 acc_1 acc_2 (uf : acc_unknown_))

instance ProtoMessage ComputeProvider where
  protoMessageName _ = "temporal.api.compute.v1.ComputeProvider"
  protoPackageName _ = "temporal.api.compute.v1"
  protoDefaultValue = defaultComputeProvider
  protoFileDescriptorBytes _ = fileDescriptorProtoBytes
  protoFieldDescriptors _ = Map.fromList
    [ (1, SomeField FieldDescriptor
        { fdName = "type"
        , fdNumber = 1
        , fdTypeDesc = ScalarType StringField
        , fdLabel = LabelOptional
        , fdGet = computeProviderType
        , fdSet = \v m -> m { computeProviderType = v }
        }), (2, SomeField FieldDescriptor
        { fdName = "details"
        , fdNumber = 2
        , fdTypeDesc = MessageType "temporal.api.common.v1.Payload"
        , fdLabel = LabelOptional
        , fdGet = computeProviderDetails
        , fdSet = \v m -> m { computeProviderDetails = v }
        })
    , (10, SomeField FieldDescriptor
        { fdName = "nexus_endpoint"
        , fdNumber = 10
        , fdTypeDesc = ScalarType StringField
        , fdLabel = LabelOptional
        , fdGet = computeProviderNexusEndpoint
        , fdSet = \v m -> m { computeProviderNexusEndpoint = v }
        })
    ]

instance IsMessage ComputeProvider

instance Aeson.ToJSON ComputeProvider where
  toJSON msg = jsonObject
      [ "type" .=: msg.computeProviderType
      , "details" .=: msg.computeProviderDetails
      , "nexusEndpoint" .=: msg.computeProviderNexusEndpoint
      ]

instance Aeson.FromJSON ComputeProvider where
  parseJSON = Aeson.withObject "ComputeProvider" $ \obj -> do
    fld_computeProviderType <- parseFieldMaybe obj "type"
    fld_computeProviderDetails <- parseFieldMaybe obj "details"
    fld_computeProviderNexusEndpoint <- parseFieldMaybe obj "nexusEndpoint"
    pure defaultComputeProvider
      { computeProviderType = maybe (computeProviderType defaultComputeProvider) id fld_computeProviderType
      , computeProviderDetails = maybe (computeProviderDetails defaultComputeProvider) id fld_computeProviderDetails
      , computeProviderNexusEndpoint = maybe (computeProviderNexusEndpoint defaultComputeProvider) id fld_computeProviderNexusEndpoint
      }

instance Hashable ComputeProvider where
  hashWithSalt salt msg = hashWithSalt (hashWithSalt (hashWithSalt (salt) msg.computeProviderType) msg.computeProviderDetails) msg.computeProviderNexusEndpoint

instance Proto.Extension.HasExtensions ComputeProvider where
  messageUnknownFields = computeProviderUnknownFields
  setMessageUnknownFields !ufs msg = msg { computeProviderUnknownFields = ufs }

instance Semigroup ComputeProvider where
  a <> b = ComputeProvider
    { computeProviderType = if b.computeProviderType == "" then a.computeProviderType else b.computeProviderType
    , computeProviderDetails = case b.computeProviderDetails of { Nothing -> a.computeProviderDetails; x -> x }
    , computeProviderNexusEndpoint = if b.computeProviderNexusEndpoint == "" then a.computeProviderNexusEndpoint else b.computeProviderNexusEndpoint
    , computeProviderUnknownFields = a.computeProviderUnknownFields <> b.computeProviderUnknownFields
    }

instance Monoid ComputeProvider where
  mempty = defaultComputeProvider