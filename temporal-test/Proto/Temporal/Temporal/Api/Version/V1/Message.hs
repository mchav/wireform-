{-# LANGUAGE StrictData #-}
{-# LANGUAGE DeriveGeneric #-}
{-# LANGUAGE DeriveAnyClass #-}
{-# LANGUAGE DerivingStrategies #-}
{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE BangPatterns #-}
{-# LANGUAGE OverloadedRecordDot #-}
-- | Auto-generated protobuf types from package @temporal.api.version.v1@.
--
-- Generated by hs-proto. Do not edit.
module Proto.Temporal.Temporal.Api.Version.V1.Message where

import Data.ByteString (ByteString)
import qualified Data.ByteString as BS
import qualified Data.ByteString.Builder as B
import Data.Int (Int32, Int64)
import Data.Text (Text)
import qualified Data.Text as T
import Data.Word (Word32, Word64)
import qualified Data.Map.Strict as Map
import qualified Data.Vector as V
import qualified Data.Vector.Unboxed as VU
import GHC.Generics (Generic)
import Control.DeepSeq (NFData(..))
import Proto.Encode
import Proto.Decode
import Proto.JSON
import Proto.Wire (Tag(..), WireType(..))
import Proto.Wire.Encode (putTag, putVarint, putFixed32, putFixed64,
  putFloat, putDouble, putText, putByteString, putLengthDelimited,
  putSVarint32, putSVarint64, putVarintSigned,
  varintSize, tagSize, fieldMessageSize,
  fieldVarintSize, fieldFixed32Size, fieldFixed64Size,
  fieldBoolSize, fieldFloatSize, fieldDoubleSize,
  fieldTextSize, fieldBytesSize)
import Proto.Google.Protobuf.Timestamp (Timestamp)
import Proto.Temporal.Temporal.Api.Enums.V1.Common (Severity)


data ReleaseInfo = ReleaseInfo
  { releaseInfoVersion :: !Text
  , releaseInfoReleasetime :: !(Maybe Timestamp)
  , releaseInfoNotes :: !Text
  }
  deriving stock (Show, Eq, Generic)
  deriving anyclass NFData

defaultReleaseInfo :: ReleaseInfo
defaultReleaseInfo = ReleaseInfo
  { releaseInfoVersion = ""
  , releaseInfoReleasetime = Nothing
  , releaseInfoNotes = ""
  }

instance MessageEncode ReleaseInfo where
  buildMessage msg =
    (if msg.releaseInfoVersion == T.empty then mempty else encodeFieldString 1 msg.releaseInfoVersion)
    <> (maybe mempty (\v -> encodeFieldMessage 2 v) msg.releaseInfoReleasetime)
    <> (if msg.releaseInfoNotes == T.empty then mempty else encodeFieldString 3 msg.releaseInfoNotes)

instance MessageSize ReleaseInfo where
  messageSize msg =
    (if msg.releaseInfoVersion == T.empty then 0 else fieldTextSize 1 msg.releaseInfoVersion)
    + (maybe 0 (\v -> fieldMessageSize 2 (messageSize v)) msg.releaseInfoReleasetime)
    + (if msg.releaseInfoNotes == T.empty then 0 else fieldTextSize 3 msg.releaseInfoNotes)

instance MessageDecode ReleaseInfo where
  messageDecoder = loop "" Nothing ""
    where
      loop acc_0 acc_1 acc_2 = do
        mTag <- getTagOr
        case mTag of
          Nothing -> pure (ReleaseInfo {releaseInfoVersion = acc_0, releaseInfoReleasetime = acc_1, releaseInfoNotes = acc_2})
          Just (Tag fn wt) -> case fn of
            1 -> do
              v <- decodeFieldString
              loop v acc_1 acc_2
            2 -> do
              v <- decodeFieldMessage
              loop acc_0 (Just v) acc_2
            3 -> do
              v <- decodeFieldString
              loop acc_0 acc_1 v
            _ -> skipField wt >> loop acc_0 acc_1 acc_2

instance ProtoToJSON ReleaseInfo where
  protoToJSON msg = jsonObject
      [ "version" .= msg.releaseInfoVersion
      , "releaseTime" .= msg.releaseInfoReleasetime
      , "notes" .= msg.releaseInfoNotes
      ]

instance ProtoFromJSON ReleaseInfo where
  protoFromJSON (JsonObject obj) = do
    v_releaseInfoVersion <- obj .:? "version"
    v_releaseInfoReleasetime <- obj .:? "releaseTime"
    v_releaseInfoNotes <- obj .:? "notes"
    pure (ReleaseInfo {
       releaseInfoVersion = v_releaseInfoVersion
      , releaseInfoReleasetime = v_releaseInfoReleasetime
      , releaseInfoNotes = v_releaseInfoNotes
    })
  protoFromJSON _ = Left "Expected JSON object"

data Alert = Alert
  { alertMessage :: !Text
  , alertSeverity :: !Severity
  }
  deriving stock (Show, Eq, Generic)
  deriving anyclass NFData

defaultAlert :: Alert
defaultAlert = Alert
  { alertMessage = ""
  , alertSeverity = (toEnum 0)
  }

instance MessageEncode Alert where
  buildMessage msg =
    (if msg.alertMessage == T.empty then mempty else encodeFieldString 1 msg.alertMessage)
    <> (if fromEnum msg.alertSeverity == 0 then mempty else encodeFieldVarint 2 (fromIntegral (fromEnum msg.alertSeverity)))

instance MessageSize Alert where
  messageSize msg =
    (if msg.alertMessage == T.empty then 0 else fieldTextSize 1 msg.alertMessage)
    + (if fromEnum msg.alertSeverity == 0 then 0 else fieldVarintSize 2 (fromIntegral (fromEnum msg.alertSeverity)))

instance MessageDecode Alert where
  messageDecoder = loop "" (toEnum 0)
    where
      loop acc_0 acc_1 = do
        mTag <- getTagOr
        case mTag of
          Nothing -> pure (Alert {alertMessage = acc_0, alertSeverity = acc_1})
          Just (Tag fn wt) -> case fn of
            1 -> do
              v <- decodeFieldString
              loop v acc_1
            2 -> do
              v <- decodeFieldEnum
              loop acc_0 v
            _ -> skipField wt >> loop acc_0 acc_1

instance ProtoToJSON Alert where
  protoToJSON msg = jsonObject
      [ "message" .= msg.alertMessage
      , "severity" .= msg.alertSeverity
      ]

instance ProtoFromJSON Alert where
  protoFromJSON (JsonObject obj) = do
    v_alertMessage <- obj .:? "message"
    v_alertSeverity <- obj .:? "severity"
    pure (Alert {
       alertMessage = v_alertMessage
      , alertSeverity = v_alertSeverity
    })
  protoFromJSON _ = Left "Expected JSON object"

data VersionInfo = VersionInfo
  { versionInfoCurrent :: !(Maybe ReleaseInfo)
  , versionInfoRecommended :: !(Maybe ReleaseInfo)
  , versionInfoInstructions :: !Text
  , versionInfoAlerts :: !(V.Vector Alert)
  , versionInfoLastupdatetime :: !(Maybe Timestamp)
  }
  deriving stock (Show, Eq, Generic)
  deriving anyclass NFData

defaultVersionInfo :: VersionInfo
defaultVersionInfo = VersionInfo
  { versionInfoCurrent = Nothing
  , versionInfoRecommended = Nothing
  , versionInfoInstructions = ""
  , versionInfoAlerts = V.empty
  , versionInfoLastupdatetime = Nothing
  }

instance MessageEncode VersionInfo where
  buildMessage msg =
    (maybe mempty (\v -> encodeFieldMessage 1 v) msg.versionInfoCurrent)
    <> (maybe mempty (\v -> encodeFieldMessage 2 v) msg.versionInfoRecommended)
    <> (if msg.versionInfoInstructions == T.empty then mempty else encodeFieldString 3 msg.versionInfoInstructions)
    <> V.foldl' (\acc v -> acc <> encodeFieldMessage 4 v) mempty msg.versionInfoAlerts
    <> (maybe mempty (\v -> encodeFieldMessage 5 v) msg.versionInfoLastupdatetime)

instance MessageSize VersionInfo where
  messageSize msg =
    (maybe 0 (\v -> fieldMessageSize 1 (messageSize v)) msg.versionInfoCurrent)
    + (maybe 0 (\v -> fieldMessageSize 2 (messageSize v)) msg.versionInfoRecommended)
    + (if msg.versionInfoInstructions == T.empty then 0 else fieldTextSize 3 msg.versionInfoInstructions)
    + (V.foldl' (\acc v -> acc + fieldMessageSize 4 (messageSize v)) 0 msg.versionInfoAlerts)
    + (maybe 0 (\v -> fieldMessageSize 5 (messageSize v)) msg.versionInfoLastupdatetime)

instance MessageDecode VersionInfo where
  messageDecoder = loop Nothing Nothing "" V.empty Nothing
    where
      loop acc_0 acc_1 acc_2 acc_3 acc_4 = do
        mTag <- getTagOr
        case mTag of
          Nothing -> pure (VersionInfo {versionInfoCurrent = acc_0, versionInfoRecommended = acc_1, versionInfoInstructions = acc_2, versionInfoAlerts = acc_3, versionInfoLastupdatetime = acc_4})
          Just (Tag fn wt) -> case fn of
            1 -> do
              v <- decodeFieldMessage
              loop (Just v) acc_1 acc_2 acc_3 acc_4
            2 -> do
              v <- decodeFieldMessage
              loop acc_0 (Just v) acc_2 acc_3 acc_4
            3 -> do
              v <- decodeFieldString
              loop acc_0 acc_1 v acc_3 acc_4
            4 -> do
              v <- decodeFieldMessage
              loop acc_0 acc_1 acc_2 (acc_3 <> V.singleton v) acc_4
            5 -> do
              v <- decodeFieldMessage
              loop acc_0 acc_1 acc_2 acc_3 (Just v)
            _ -> skipField wt >> loop acc_0 acc_1 acc_2 acc_3 acc_4

instance ProtoToJSON VersionInfo where
  protoToJSON msg = jsonObject
      [ "current" .= msg.versionInfoCurrent
      , "recommended" .= msg.versionInfoRecommended
      , "instructions" .= msg.versionInfoInstructions
      , "alerts" .= msg.versionInfoAlerts
      , "lastUpdateTime" .= msg.versionInfoLastupdatetime
      ]

instance ProtoFromJSON VersionInfo where
  protoFromJSON (JsonObject obj) = do
    v_versionInfoCurrent <- obj .:? "current"
    v_versionInfoRecommended <- obj .:? "recommended"
    v_versionInfoInstructions <- obj .:? "instructions"
    v_versionInfoAlerts <- obj .:? "alerts"
    v_versionInfoLastupdatetime <- obj .:? "lastUpdateTime"
    pure (VersionInfo {
       versionInfoCurrent = v_versionInfoCurrent
      , versionInfoRecommended = v_versionInfoRecommended
      , versionInfoInstructions = v_versionInfoInstructions
      , versionInfoAlerts = v_versionInfoAlerts
      , versionInfoLastupdatetime = v_versionInfoLastupdatetime
    })
  protoFromJSON _ = Left "Expected JSON object"