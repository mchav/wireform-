-- | Convenience re-exports for HTML5 parsing and serialization.
module Wireform.HTML
  ( module HTML.Value
  , module HTML.Parse
  , module HTML.Encode
  , module HTML.Query
  , module HTML.Class
  ) where

import HTML.Value
import HTML.Parse
import HTML.Encode
import HTML.Query
import HTML.Class
